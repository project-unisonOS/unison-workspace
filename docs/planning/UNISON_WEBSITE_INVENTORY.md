# Unison public website inventory

Status: Phase 1 review candidate
Inventory date: 2026-07-21

## Disposition vocabulary

- **Preserve**: accurate reference material that remains useful.
- **Rewrite**: retained subject with claims or framing changed in Phase 0.
- **Redirect**: compatibility URL generated by MkDocs redirects.
- **Claim review**: useful historical page whose implementation/maturity claims need later line-by-line review.
- **New**: Phase 0 product-truth foundation.

## Inventory

| Area | Pages | Disposition | Phase 0 action |
| --- | --- | --- | --- |
| Product foundation | `index`, `vision`, `how-unison-works`, `personal-node`, `household-context-spaces`, `privacy-security`, `accessibility`, `current-status`, `terminology` | Rewrite/New | Reframed around the approved personal node, per-person assistants, explicit context spaces, person-aligned economics, truthful maturity, and accessibility. |
| Architecture | `architecture/overview`, `architecture/deep-dive` | Claim review | Preserved as engineering history; the new “How Unison works” page carries the approved six-boundary public model. Full deep-page reconciliation continues with the relevant implementation phase. |
| Experience | `experience`, `experience/*` | Claim review | Preserved; public entry pages no longer imply all modalities are complete. Specialized doctrine/prompt pages remain unlisted engineering references. |
| Installation | `developers/install-*`, `install-options`, `releases` | Claim review | Preserved with the public status page stating that no supported appliance image exists. Installation claims require release evidence before promotion. |
| Developer guides | `developers/get-started`, `prerequisites`, `workspace-and-repos`, `devstack`, `renderer`, `testing`, workflow/build/contributing pages | Preserve/Claim review | Retained for contributors; stale redirect aliases remain generated. |
| Reference | `reference/specs`, `apis`, `security`, `compatibility` | Claim review | Retained as engineering reference; canonical machine schemas are now declared in `unison-common/schemas`. |
| Project | `project/governance`, `roadmap`, `license`, `bci-overview` | Preserve/Claim review | Governance/license preserved. Roadmap and BCI material remain non-implementation claims and are subordinate to current status. |
| Compatibility URLs | `vision.md` legacy route, `developers/evaluate-alpha`, `developers/renderer-and-shell` | Redirect | MkDocs emits redirects; redirect stubs are excluded from duplicate accessibility audits. |

## Phase 1 claim delta

`current-status` now identifies Phase 0 as complete and Phase 1 as a review
candidate. It lists the implemented local identity/principal foundations while
keeping relationship context spaces, remote channels, backup, and the supported
appliance explicitly incomplete. `terminology` now distinguishes Person,
Principal, Assistant instance, Household membership, and Channel identity.

## Verification

The audit discovers every substantive generated `index.html`, rather than using
a stale URL list. Internal build completeness is enforced by MkDocs strict mode;
Chromium plus axe checks every discovered page. Redirect stubs are generated and
tested through their target pages.
